import { PersonalProjects } from "./PersonalProjects";
import './projects.css'

export const Projects = () => {
    return (
        <section className="ProjectsWrapper">
            <PersonalProjects />
        </section>
    );
}